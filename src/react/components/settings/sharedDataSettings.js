import React from 'react';
import { Typography } from '@material-ui/core';

export default function SharedDataSettings() {
	return (
		<Typography variant="h2">This is the Shared Data Settings Content</Typography>
	);
}
