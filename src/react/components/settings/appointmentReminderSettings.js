import React from 'react';
import { Typography } from '@material-ui/core';

export default function AppointmentRemindersSettings() {
	return (
		<Typography variant="h2">This is the Appointment Reminder Settings Content</Typography>
	);
}
