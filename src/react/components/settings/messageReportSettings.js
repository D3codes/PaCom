import React from 'react';
import { Typography } from '@material-ui/core';

export default function MessageReportSettings() {
	return (
		<Typography variant="h2">This is the Message Report Settings Content</Typography>
	);
}
