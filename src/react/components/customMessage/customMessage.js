import React from 'react';
import { Typography } from '@material-ui/core';

export default function CustomMessage() {
	return (
		<Typography variant="h2">This is the Custom Message Content</Typography>
	);
}
