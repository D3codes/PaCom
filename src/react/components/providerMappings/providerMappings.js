import React from 'react';
import { Typography } from '@material-ui/core';

export default function ProviderMappings() {
	return (
		<Typography variant="h2">This is the Provider Mappings Content</Typography>
	);
}
